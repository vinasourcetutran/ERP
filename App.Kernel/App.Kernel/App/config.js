define(function () {
    var config, modules=['Kernel','Core'];
    config = {
        appIcon: '/content/images/icon.png',
        appName: 'SPA Demo',
        defaultUrl: 'Kernel/index',
        modules: modules
    };
    return config;
});