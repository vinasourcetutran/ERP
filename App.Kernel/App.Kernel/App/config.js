define(function () {
    var config, modules = ['Kernel', 'tenancy'];
    config = {
        appIcon: '/content/images/icon.png',
        appName: 'SPA Demo',
        defaultUrl: 'kernel/index',
        modules: modules
    };
    return config;
});